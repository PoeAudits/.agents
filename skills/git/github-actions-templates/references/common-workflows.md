# Common workflow patterns

Load this reference when needing reusable GitHub Actions patterns beyond the inline templates.

## Patterns

- Lint + test on pull requests.
- Deploy on tags.
- Use environment protection rules for production deploys.
- Cache dependency installs.
